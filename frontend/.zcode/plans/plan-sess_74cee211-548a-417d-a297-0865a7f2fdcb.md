## Problem
1. **Header logo doesn't show in Gmail** — inline `<svg>` is stripped by Gmail/Outlook/Yahoo. Hosted URL won't work either because `farmdeck.app` isn't deployed yet (DNS doesn't resolve). Fix = **inline CID attachment** (PNG embedded inside the email, referenced as `cid:farmdeck-logo`).
2. **"Open Farmdeck" button is hardcoded** to `https://farmdeck.app`. Fix = new configurable `APP_URL` so the button opens the real frontend URL in any environment.

## Changes

### 1. Config — add `APP_URL` (button/frontend URL)
- **`backend/internal/config/config.go`**: add field `AppURL string \`env:"APP_URL" envDefault:"http://localhost:5173"\``.
- **`backend/configs/.env`**: add `APP_URL=http://localhost:5173` (set to `https://farmdeck.app` in prod).

### 2. Template data — add `AppURL`
- **`backend/pkg/mailer/templates/templates.go`**: add `AppURL string` to the `Welcome` struct.

### 3. Logo asset — generate a transparent PNG and embed it
- Author **`backend/pkg/mailer/templates/email-logo.svg`** (transparent background, mark + white "Farm" + green "deck" wordmark, ~2× for retina).
- Rasterize to **`backend/pkg/mailer/templates/logo.png`** via `qlmanage` (the only SVG→PNG tool available on this machine).
- Embed it with `//go:embed logo.png` in `templates.go`; expose via `LogoPNG() []byte`.

### 4. Mailer — support inline attachments
- **`backend/pkg/mailer/mailer.go`**: add `Attachment` type (`Content []byte`, `Filename`, `ContentType`, `ContentID`) and `Attachments []Attachment` field on `Message`.
- **`backend/pkg/mailer/resend.go`**: map `Message.Attachments` → `[]*resend.Attachment` (using `Content` + `ContentId`), so the CID image ships inside the email.

### 5. Welcome template — use CID image + configurable URL
- **`welcome.html`**: replace the `<svg>…</svg>` header with `<img src="cid:farmdeck-logo" width="168" height="40" alt="Farmdeck">` on the existing dark-green banner, plus a styled text fallback. Change button `href` and footer link to `{{.AppURL}}` (defensive: defaults to `https://farmdeck.app` if empty via `{{if .AppURL}}…{{else}}…{{end}}`).
- **`welcome.txt`**: use `{{.AppURL}}` for the link.

### 6. Foolproof helper (so the logo is never forgotten)
- Add `templates.WelcomeMessage(to string, data Welcome) (mailer.Message, error)` in `templates.go` — renders HTML + text, attaches the logo PNG with `ContentID: "farmdeck-logo"`, and returns a ready-to-send `Message`. This makes the correct path (icon always attached) the one-line path. *(No existing callers to break — the mailer isn't wired into registration yet.)*

### 7. Tests
- Update **`welcome_render_test.go`**: assert `{{.AppURL}}` renders into button href and text link; assert HTML references `cid:farmdeck-logo`.

## Verification
- `go build ./...` (full backend)
- `go test ./pkg/mailer/...`
- Render preview and confirm `<img src="cid:farmdeck-logo">` + `href="<appurl>"` present.

## Notes / assumptions
- `APP_URL` defaults to `http://localhost:5173` (matches current `CORS_ALLOWED_ORIGINS`). Set `APP_URL=https://farmdeck.app` in prod. A button pointing to localhost is only useful on your own machine during testing — expected in dev.
- **Not** wiring the mailer into the registration flow (out of scope; you didn't ask). When you do wire it, call `templates.WelcomeMessage(to, Welcome{Name: u.Name, AppURL: cfg.AppURL})` then `mailer.SendAsync(...)`.
- `MAIL_FROM_ADDRESS` still says "Farm Deck" (two words) + Resend's sandbox sender `onboarding@resend.dev` — leaving as-is; flag to align to "Farmdeck" + your verified domain before going live.